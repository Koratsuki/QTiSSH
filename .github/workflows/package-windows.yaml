name: Package (Windows)

on:
  push:
    tags:
      - 'v*'
    branches:
      - main
  workflow_dispatch:

jobs:
  build-windows:
    name: Build Windows
    runs-on: windows-2022
    permissions:
      contents: write
    env:
      QT_VERSION: 6.7.2
      QT_MODULES: qtcharts
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Install Ninja
        run: choco install ninja -y

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          modules: ${{ env.QT_MODULES }}
          cache: true

      - name: Install OpenSSL
        run: choco install openssl -y

      - name: Show OpenSSL install
        shell: bash
        run: |
          echo "=== Checking common OpenSSL paths ==="
          ls -la "/c/Program Files/" | grep -i openssl || true
          ls -la "/c/Program Files/OpenSSL-Win64/" || true
          ls -la "/c/Program Files/OpenSSL-Win64/lib/" || true
          ls -la "/c/Program Files/OpenSSL-Win64/include/" || true
          # Also check choco lib
          find "/c/ProgramData/chocolatey" -name "libssl*" 2>/dev/null || true
          find "/c/ProgramData/chocolatey" -name "libcrypto*" 2>/dev/null || true

      - name: Extract version from tag
        id: version
        shell: bash
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          else
            VERSION=$(grep 'project(QTiSSH VERSION' src/CMakeLists.txt | sed 's/.*VERSION \([0-9.]*\).*/\1/')
            echo "VERSION=${VERSION}" >> $GITHUB_OUTPUT
          fi

      - name: Configure
        shell: cmd
        run: |
          cmake -S src -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH="%Qt6_DIR%" -DOPENSSL_ROOT_DIR="C:\Program Files\OpenSSL-Win64"

      - name: Build
        run: cmake --build build

      - name: Rename binary
        shell: bash
        run: |
          cp build/QTiSSH.exe "QTiSSH-v${{ steps.version.outputs.VERSION }}-x86_64.exe"

      - name: Show packages
        run: Get-ChildItem -Filter *.exe | Select-Object Name, Length

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: qtissh-windows
          path: "*.exe"

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: "*.exe"
